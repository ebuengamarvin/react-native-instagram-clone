import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  icon: {
    position: "absolute",
    top: 55,
    right: 5,
    borderColor: "#fff",
    borderWidth: 2,
    borderRadius: 50,
    backgroundColor: "#fff",
  },
});

export default styles;
