import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 50,
    borderWidth: 3,
    // borderColor: "#ae22e0",
  },
  image: {
    borderRadius: 50,
    borderWidth: 1,
    borderColor: "#ffffff",
  },
});
